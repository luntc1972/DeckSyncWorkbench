@model DeckFlow.Web.Models.FeedbackSubmission
@{
    ViewData["Title"] = "Send Feedback to DeckFlow";
    ViewData["Description"] = "Send feedback, bug reports, or feature ideas for DeckFlow's Magic: The Gathering deck-analysis tools. We read every submission.";
    var success = TempData["FeedbackSuccess"] as bool? ?? false;
}

<section class="feedback-page">
    <div class="feedback-panel">
        <div class="feedback-heading">
            <span class="feedback-eyebrow">DeckFlow community</span>
            <h1>Send Feedback</h1>
            <p>Report a bug, suggest a feature, or just share a thought. Email is optional.</p>
        </div>

        @if (success)
        {
            <div class="feedback-banner feedback-banner--success" role="status">
                Thanks — your feedback was received.
            </div>
        }

        @* Why: no `novalidate` — the native constraints below (required, minlength=10, type=email)
           are the only client-side validation this page has; the server re-checks in FeedbackSubmission. *@
        <form method="post" asp-action="Index" asp-antiforgery="true" class="feedback-form">
            <div class="feedback-field">
                <label asp-for="Type">Type</label>
                <select asp-for="Type" asp-items="Html.GetEnumSelectList<DeckFlow.Web.Models.FeedbackType>()" data-df-select></select>
                <span asp-validation-for="Type" class="feedback-error"></span>
            </div>

            <div class="feedback-field">
                <label asp-for="Message">Message</label>
                <textarea asp-for="Message" rows="8" maxlength="4000" minlength="10" required></textarea>
                <span asp-validation-for="Message" class="feedback-error"></span>
            </div>

            <div class="feedback-field">
                <label asp-for="Email">Email (optional)</label>
                <input asp-for="Email" type="email" maxlength="200" autocomplete="email" />
                <span asp-validation-for="Email" class="feedback-error"></span>
            </div>

            <div class="feedback-honeypot" aria-hidden="true">
                <label for="Website">Website</label>
                <input type="text" name="Website" id="Website" tabindex="-1" autocomplete="off" />
            </div>

            <button type="submit" class="feedback-submit">Send Feedback</button>
        </form>
    </div>
</section>

@section Scripts {
    <script src="~/js/feedback.js" asp-append-version="true"></script>
}
